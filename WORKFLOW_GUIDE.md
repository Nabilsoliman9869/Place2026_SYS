# دليل دورة العمل المتكاملة - نظام Place2026

هذا المستند يوثق دورة العمل الكاملة (End-to-End Workflow) للنظام، بدءاً من استقطاب المهتمين وصولاً إلى توظيفهم، مع شرح دور كل موديول.

---

## 🔄 ملخص الدورة (The Cycle)
1. **الاستقطاب (Acquisition):** تسجيل اهتمام عبر الإعلانات/الموقع.
2. **التأهيل (Qualification):** تحويل المهتم إلى مرشح بعد التواصل معه.
3. **التقييم (Assessment):** إجراء امتحانات تحديد مستوى.
4. **التطوير (Development):** التدريب وتنمية المهارات.
5. **التشبيك (Matching):** المطابقة مع طلبات العملاء.
6. **التوظيف (Hiring):** المقابلات والتعيين النهائي.

---

## 📝 التفاصيل خطوة بخطوة

### 1. مرحلة التسجيل (Landing & Sales)
- **المدخل:** يقوم الشخص بتسجيل بياناته (الاسم، الهاتف) عبر صفحة `Landing Page`.
- **المنطق:**
  - يُسمح بتكرار التسجيل لنفس الشخص لاهتمامات مختلفة.
  - يتم منع التكرار اللحظي (Spam) لنفس اليوم.
- **الإجراء:** يظهر في "لوحة المبيعات" كـ `New Lead`.

### 2. مرحلة المبيعات والتحويل (Sales Dashboard)
- **الهدف:** تصفية الجادين وتحويلهم لمرشحين.
- **الإجراءات:**
  - موظف المبيعات يتصل بالمهتم ويغير حالته إلى `Contacted`.
  - عند التأكد من جديته، يتم تحويله إلى `Candidate` (مرشح).
- **الذكاء:** النظام يفحص تلقائياً؛ إذا كان المرشح موجوداً، يدمج الاهتمام الجديد في ملفه القديم بدلاً من تكرار الملفات.

### 3. مرحلة التقييم (Exams)
- **الهدف:** تحديد مستوى المرشح قبل التدريب.
- **الإجراء:** حجز موعد امتحان تحديد مستوى (Placement Test) من شاشة `Exam Scheduling`.
- **النتيجة:** درجة الامتحان تحدد المسار التدريبي المناسب.

### 4. مرحلة التدريب (Trainings)
- **التسجيل:** إلحاق المرشح بدورات (Customer Service, English, etc.) من شاشة `Trainings`.
- **الحضور:** استخدام نظام `QR Code` لتسجيل الحضور اليومي في الجلسات.
- **المالية:** إنشاء فواتير التدريب ومتابعة سدادها من شاشة `Invoices`.

### 5. مرحلة العملاء والمطابقة (Clients & Matching)
- **العميل:** شركة تطلب موظفين (مثلاً: مطلوب 5 محاسبين).
- **المطابقة:**
  - الدخول لملف العميل وفتح "طلبات التوظيف".
  - استخدام زر "مطابقة" للبحث عن أنسب المرشحين بناءً على (المهارات، التعليم، التقييمات).
- **المخرجات:** قائمة مختصرة (Shortlist) للمقابلات.

### 6. التوظيف والإغلاق (Hiring)
- **المقابلات:** جدولة مقابلات العمل.
- **التعيين:** تسجيل `Hiring Record` عند قبول المرشح، مما يغلق الدورة بنجاح.

---

## 💡 نواقص وتحسينات مقترحة للكود (To-Do)

1. **المرشد التفاعلي (Onboarding Tour):**
   - إضافة شرح منبثق (Tooltip) عند فتح كل شاشة لأول مرة يشرح وظيفتها.
   
2. **تحسين تجربة الجوال:**
   - التأكد من أن الجداول `DataTable` قابلة للتمرير (Scrollable) بشكل جيد على الشاشات الصغيرة.

3. **الإشعارات الآلية:**
   - إرسال إشعار (وهمي حالياً) عند اقتراب موعد امتحان أو مقابلة.

4. **لوحة تحكم المدير الشاملة:**
   - تجميع أهم الأرقام (الإيرادات، عدد المتدربين الحاليين) في شاشة واحدة محدثة لحظياً.

---
*تم التوثيق بتاريخ: 2026-01-07*
